NODE_ENV=test
PORT=4000
API_PREFIX=api

POSTGRES_USER=username
POSTGRES_PASSWORD=password
POSTGRES_HOST=localhost
POSTGRES_DB=mydb
POSTGRES_PORT=5444 # Needs to match the port in docker-compose-test.yml

DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public

JWT_SECRET=YOUR_SECRET_KEY_HERE
JWT_TOKEN_AUDIENCE=localhost:4000
JWT_TOKEN_ISSUER=localhost:4000
JWT_ACCESS_TOKEN_TTL=3600
JWT_REFRESH_TOKEN_TTL=86400

AWS_S3_REGION=test
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_S3_BUCKET_NAME=test
AWS_S3_MAX_FILE_SIZE=5242880
AWS_S3_ENDPOINT=https://localhost:3000
AWS_S3_PUBLIC_URL=https://localhost:3000/